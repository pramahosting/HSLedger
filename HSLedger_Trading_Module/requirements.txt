# HSLedger — Trading Module
# Module-specific requirements
# All versions aligned with the project root requirements.txt
#
# Install:
#   pip install -r trading/requirements.txt
#
# Note: pandas, openpyxl, streamlit, fastapi and most other packages
# are already listed in the project root requirements.txt.
# This file lists only the packages the trading module depends on,
# pinned to the same versions already in use across the project.

# ── Core data processing ──────────────────────────────────────────────────────
pandas==2.3.3
numpy==2.4.3
python-dateutil==2.9.0.post0
pytz==2026.1.post1

# ── Excel I/O ─────────────────────────────────────────────────────────────────
openpyxl==3.1.5
et_xmlfile==2.0.0           # openpyxl dependency

# ── Streamlit (frontend) ──────────────────────────────────────────────────────
streamlit==1.55.0
altair==6.0.0
blinker==1.9.0
cachetools==7.0.5
narwhals==2.18.0
packaging==26.0
pydeck==0.9.1
pyarrow==23.0.1
tornado==6.5.5
watchdog==6.0.0

# ── FastAPI (backend integration) ────────────────────────────────────────────
fastapi==0.135.1
uvicorn==0.42.0
starlette==0.52.1
anyio==4.12.1
h11==0.16.0

# ── Data validation ───────────────────────────────────────────────────────────
pydantic==2.12.5
pydantic_core==2.41.5
annotated-types==0.7.0
typing_extensions==4.15.0
typing-inspection==0.4.2

# ── Utilities ─────────────────────────────────────────────────────────────────
python-dotenv==1.2.2
click==8.3.1
six==1.17.0
attrs==25.4.0
certifi==2026.2.25
charset-normalizer==3.4.6
idna==3.11
urllib3==2.6.3
requests==2.32.5

# ── Testing ───────────────────────────────────────────────────────────────────
pytest>=7.0.0               # not pinned — use whatever is available
